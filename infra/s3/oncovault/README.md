# OncoVault Warehouse Bucket

Thy shall be referred to as `oncovault warehouse` bucket.

This stack provisions the S3 bucket for OncoVault iceberg tables purpose.

```
export AWS_PROFILE=unimelb-warehouse-prod-admin

terraform init
terraform plan
terraform apply
```
