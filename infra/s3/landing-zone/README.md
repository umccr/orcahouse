# Landing Zone Bucket

Thy shall be referred to as `lz` bucket.

This stack provisions the S3 bucket for OrcaHouse data landing zone purpose.

```
export AWS_PROFILE=unimelb-warehouse-prod-admin

terraform init
terraform plan
terraform apply
```
