# Aurora PostgreSQL for Data API backend – dev

Login to the warehouse AWS account and apply like so.

```
export AWS_PROFILE=unimelb-warehouse-prod-admin

terraform init
terraform plan
terraform apply
```
