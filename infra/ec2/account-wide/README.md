# Data Warehouse account-wide resources for EC2 provisioning

The account-wide prerequisite resources that are needed for EC2 provisioning.

```
export AWS_PROFILE=unimelb-warehouse-prod-admin

terraform init
terraform plan
terraform apply
```
